---
"@bcl32/hooks": major
---

Remove `useBokehChart` (dead since `BokehLineChart` was removed from
`@bcl32/charts` in 3.0.0). `useBokehChart`, `BokehChartData`, and
`GraphOptions` are no longer exported from `@bcl32/hooks`.
