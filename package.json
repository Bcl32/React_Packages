{
  "name": "bcl32-react-packages",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm run build && pnpm -r publish --no-git-checks"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  }
}
